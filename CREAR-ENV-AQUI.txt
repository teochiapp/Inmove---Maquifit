╔═══════════════════════════════════════════════════════════════╗
║  🔧 INSTRUCCIONES: CREAR ARCHIVO .env                         ║
╚═══════════════════════════════════════════════════════════════╝

📁 UBICACIÓN: Crea el archivo .env en esta misma carpeta (maquifit/)
   La ubicación debe ser: maquifit/.env

📝 CONTENIDO DEL ARCHIVO .env:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# EmailJS Configuration
REACT_APP_EMAILJS_SERVICE_ID=service_2owznmm
REACT_APP_EMAILJS_TEMPLATE_ID=template_pjck9zy
REACT_APP_EMAILJS_PUBLIC_KEY=Egizjd8jOmECOzre0

# MercadoPago Configuration (agregar tu token aquí)
# REACT_APP_MERCADOPAGO_ACCESS_TOKEN=tu_access_token_aqui
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ IMPORTANTE:

1. El archivo se llama exactamente: .env (con el punto al inicio)

2. NO debe tener extensión (.txt, .js, etc.)

3. Debe estar en la carpeta maquifit/ (mismo nivel que package.json)

4. Después de crear el archivo, DEBES REINICIAR el servidor:
   - Presiona Ctrl+C en la terminal
   - Ejecuta: npm start

5. El archivo .env NO se sube a Git (ya está en .gitignore)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📧 ¿CUÁNDO SE ENVÍA EL EMAIL?

El email se envía cuando el usuario hace clic en "Continuar con el pago"
ANTES de ir a MercadoPago.

Flujo:
1. Usuario llena formulario
2. Clic en "Continuar con el pago" 
3. 📧 SE ENVÍA EMAIL (botón dice "📧 Enviando...")
4. Redirige a MercadoPago

Email destino: maquiponce96@gmail.com

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 DESPUÉS DE CREAR EL .env:

Deberías poder borrar este archivo (CREAR-ENV-AQUI.txt)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━



